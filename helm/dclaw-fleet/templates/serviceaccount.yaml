apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dclaw-fleet.serviceAccountName" . }}
  labels:
    {{- include "dclaw-fleet.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
