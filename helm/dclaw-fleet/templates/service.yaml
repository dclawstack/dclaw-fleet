apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-fleet.fullname" . }}
  labels:
    {{- include "dclaw-fleet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-fleet.selectorLabels" . | nindent 4 }}
